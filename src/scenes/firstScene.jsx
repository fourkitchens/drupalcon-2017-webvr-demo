/**
 * @file firstScene.js
 * Exports an initial scene component.
 */

import React from 'react';
import Scene from '../components/Scene.jsx';

/**
 * Returns React component containing an initial scene.
 * {@inheritdoc}
 */
const firstScene = () => (
  <Scene />
);

export default firstScene;
